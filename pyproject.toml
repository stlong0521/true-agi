[project]
name = "true-agi"
version = "0.1.0"
description = "Playground for testing Transformer-based AGI ideas"
readme = "README.md"
requires-python = ">=3.10, <3.13"
dependencies = [
    "torch==2.2.2",
    "requests",
    "numpy<2.0",
]

[tool.uv]
managed = true
